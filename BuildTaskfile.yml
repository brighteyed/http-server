version: '3'

tasks:
  default:
    cmds:
      - task: x64

  x64:
    env:
      GOARCH: "amd64"
    cmds:
      - go build -o {{.EXEFILE}} main.go

  x86:
    env:
      GOARCH: "386"
    cmds:
      - go build -o {{.EXEFILE}} main.go
